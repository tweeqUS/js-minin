"use strict"

const CarName = "Mitsubishi";
let maxCarSpeed = "240км/ч",
    carOwner = 'Roman';

    alert(CarName);
    alert(maxCarSpeed);
    alert(carOwner);

    maxCarSpeed = "200км/ч";
    console.log('У машины изменилась скорость', maxCarSpeed);

    maxCarSpeed = '190км/ч';
    console.log('У машины изменилась скорость', maxCarSpeed);

    maxCarSpeed = '180км/ч';
    console.log('У машины изменилась скорость', maxCarSpeed);
